import { expect } from 'vitest';
import { it } from 'vitest';
import { describe } from 'vitest';

describe('Assistant Resonse', () => {
  it('should send text', async () => {
    expect(true);
  });
});
